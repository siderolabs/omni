{{- if .Values.ingress.kubernetesProxy.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "omni-v2.fullname" . }}-k8s-proxy
  labels:
    {{- include "omni-v2.labels" . | nindent 4 }}
  {{- with .Values.ingress.kubernetesProxy.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ingress.kubernetesProxy.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.ingress.kubernetesProxy.tls }}
  tls:
    - hosts:
        - {{ .Values.ingress.kubernetesProxy.host | quote }}
      secretName: {{ .Values.ingress.kubernetesProxy.tls.secretName }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.kubernetesProxy.host | quote }}
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ include "omni-v2.fullname" $ }}
                port:
                  number: {{ .Values.service.main.k8sProxyPort }}
{{- end }}
