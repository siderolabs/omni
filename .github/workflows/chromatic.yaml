# THIS FILE WAS AUTOMATICALLY GENERATED BY KRES, PLEASE DO NOT EDIT.
#
# Generated on 2026-09-07T10:52:54Z by kres d15f055.

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true
'on':
  push:
    branches:
      - main
      - release-*
    tags:
      - v*
  pull_request:
    branches:
      - main
      - release-*
name: chromatic
jobs:
  chromatic:
    name: Run chromatic
    runs-on:
      group: generic
    environment:
      name: Storybook preview
      url: ${{ steps.chromatic.outputs.storybookUrl }}
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # version: v7.0.1
        with:
          fetch-depth: "0"
      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # version: v7.0.0
        with:
          node-version: 24.20.0
      - name: Install dependencies
        run: |
          npm ci --strict-allow-scripts
        working-directory: frontend/
      - name: Get token
        run: |
          chromaticProjectToken=$(sops decrypt --extract='["secrets"]["CHROMATIC_PROJECT_TOKEN"]' .secrets.yaml)
          echo "::add-mask::${chromaticProjectToken}"
          echo "CHROMATIC_PROJECT_TOKEN=${chromaticProjectToken}" >> $GITHUB_ENV
      - name: Run Chromatic
        id: chromatic
        uses: chromaui/action@2a0b63f30233c48591844a46d451b9cf68128186 # version: v18.7.2
        with:
          projectToken: ${{ env.CHROMATIC_PROJECT_TOKEN }}
          workingDir: frontend/
