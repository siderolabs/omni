# THIS FILE WAS AUTOMATICALLY GENERATED BY KRES, PLEASE DO NOT EDIT.
#
# Generated on 2026-09-03T08:21:54Z by kres 09bd9e5.

version: "2"
# options for analysis running
run:
  modules-download-mode: readonly
  issues-exit-code: 1
  tests: true
  build-tags:
    - integration
    - sidero.tools
# output configuration options
output:
  formats:
    text:
      path: stdout
      print-issued-lines: true
      print-linter-name: true
  path-prefix: ""
linters:
  default: all
  disable:
    - exhaustruct
    - err113
    - funcorder
    - funlen
    - gochecknoglobals
    - gochecknoinits
    - goconst # complains about repeated values without understanding code context, it checks comments and logger variables and gives false positives for all of those
    - godox
    - gomoddirectives
    - gomodguard
    - gosec
    - inamedparam
    - ireturn
    - mnd
    - nestif
    - nonamedreturns
    - paralleltest
    - tagalign
    - tagliatelle
    - thelper
    - varnamelen
    - wrapcheck
    - testifylint # complains about our assert recorder and has a number of false positives for assert.Greater(t, thing, 1)
    - protogetter # complains about us using Value field on typed spec, instead of GetValue which has a different signature
    - perfsprint # complains about us using fmt.Sprintf in non-performance critical code, updating just kres took too long
    - musttag # seems to be broken - goes into imported libraries and reports issues there
    - nolintlint # gives false positives - disable until https://github.com/golangci/golangci-lint/issues/3228 is resolved
    - wsl # replaced by wsl_v5
    - noinlineerr
    - embeddedstructfieldcheck # fighting in many places with fieldalignment
  # all available settings of specific linters
  settings:
    cyclop:
      # the maximal code complexity to report
      max-complexity: 20
    dogsled:
      max-blank-identifiers: 2
    dupl:
      threshold: 150
    errcheck:
      check-type-assertions: true
      check-blank: true
      exclude-functions:
        - github.com/siderolabs/omni/client/internal/safeout.Fprintf
    exhaustive:
      default-signifies-exhaustive: false
    gocognit:
      min-complexity: 30
    nestif:
      min-complexity: 5
    gocritic:
      disabled-checks: []
    gocyclo:
      min-complexity: 20
    godot:
      scope: declarations
    gomodguard_v2: {}
    govet:
      enable-all: true
    lll:
      line-length: 200
      tab-width: 4
    misspell:
      locale: US
    nakedret:
      max-func-lines: 30
    prealloc:
      simple: true
      range-loops: true # Report preallocation suggestions on range loops, true by default
      for-loops: false # Report preallocation suggestions on for loops, false by default
    revive:
      rules:
        - name: var-naming # Complains about package names like "common"
          disabled: true
    rowserrcheck: {}
    testpackage: {}
    unparam:
      check-exported: false
    unused:
      local-variables-are-used: false
    whitespace:
      multi-if: false # Enforces newlines (or comments) after every multi-line if statement
      multi-func: false # Enforces newlines (or comments) after every multi-line function signature
    wsl:
      strict-append: true
      allow-assign-and-call: true
      allow-multiline-assign: true
      allow-trailing-comment: false
      force-case-trailing-whitespace: 0
      allow-separated-leading-comment: false
      allow-cuddle-declarations: false
      force-err-cuddling: false
    depguard:
      rules:
        prevent_unmaintained_packages:
          list-mode: lax # allow unless explicitly denied
          files:
            - $all
          deny:
            - pkg: io/ioutil
              desc: "replaced by io and os packages since Go 1.16: https://tip.golang.org/doc/go1.16#ioutil"
        prevent_sync_errgroup:
          list-mode: lax
          files:
            - $all
            - "!$test"
            - "!**/integration-test/**"
            - "!**/omnictl/**"
            - "!**/panichandler/**"
          deny:
            - pkg: golang.org/x/sync/errgroup
              desc: Use github.com/siderolabs/omni/client/pkg/panichandler.ErrGroup
                instead
    forbidigo:
      forbid:
        - pattern: ^fmt\.(Print|Printf|Println)$
          msg: >-
            omnictl renders text received from the Omni API, and a terminal acts on
            control sequences in it. Print through client/internal/safeout instead.
        - pattern: ^os\.(Stdout|Stderr)$
          msg: >-
            use safeout.Stdout / safeout.Stderr so text received from the Omni API
            cannot drive the operator's terminal. A stream which must stay raw, e.g.
            under colored output, needs //nolint:forbidigo saying which.
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
    rules:
      - linters:
          - forbidigo
        path-except: ^pkg/omnictl/
      # errcheck excuses fmt.Fprint* to os.Stdout and os.Stderr, extend that to the filtered streams
      - linters:
          - errcheck
        path: ^pkg/omnictl/
        text: Error return value of `fmt\.Fprint
  enable:
    # omnictl renders text received from the Omni API, and a terminal acts on control sequences in it,
    # so all output goes through client/internal/safeout. The rules below only bite in the
    # client module, as they are scoped to the omnictl packages.
    - forbidigo
issues:
  max-issues-per-linter: 1024
  max-same-issues: 1024
  uniq-by-line: true
  new: false
severity:
  default: error
formatters:
  enable:
    - gci
    - gofmt
    - gofumpt
  settings:
    gci:
      sections:
        - standard
        - default
        - localmodule
    gofmt:
      simplify: true
    gofumpt:
      extra-rules: false
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
